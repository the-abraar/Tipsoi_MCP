

Tipsoi MCP Server connects LLMs to Tipsoi HRM so users can query employees, attendance, leave, overtime, holidays, departments, designations, and notifications, and perform approved leave actions like apply, approve, reject, and adjust through the Tipsoi API. It supports per-user OAuth, so each user works with their own Tipsoi account and permissions.