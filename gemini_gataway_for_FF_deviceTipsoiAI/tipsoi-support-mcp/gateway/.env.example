# ── Your Gemini key (server-side only — never given to users) ───────────────
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash

# ── Q&A app (gateway.qna) ───────────────────────────────────────────────────
# Comma-separated keys you hand out (simplest). Each looks like tsg_xxx
QNA_API_KEYS=
QNA_RPM=30
QNA_TOP_K=4

# ── Full MCP-backed gateway (gateway.app) ───────────────────────────────────
GATEWAY_KEYS_FILE=./keys.json
GATEWAY_MAX_TOOL_ITERS=6
# MCP_PYTHON=python

# ── Escalation backend (used by MCP server / gateway.app) ───────────────────
SUPPORT_TICKET_MODE=off
# CLICKUP_API_TOKEN=pk_...
# CLICKUP_LIST_ID=901800000000
SUPPORT_PHONE=+8809638017170
SUPPORT_EMAIL=support@inovacetech.com
